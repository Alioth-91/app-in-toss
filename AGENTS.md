# 앱인토스 프로젝트 공통 작업 규칙

이 파일은 저장소 전체에 적용되는 공통 규칙입니다. `kokkok/`, `gongjikok/` 및 앞으로 추가되는 앱인토스 프로젝트를 작업할 때 먼저 읽습니다.

## 작업 단위 : 50~100줄

- 승인 한 번당 직접 작성하는 주요 애플리케이션 코드는 대략 50~100줄을 기준으로 한다.
- 테스트, 문서, 설정, 자동 생성 파일, lockfile은 이 수치에서 제외한다.
- 하나의 개념이 50줄 미만에서 자연스럽게 끝나면 억지로 늘리지 않는다.
- 100줄을 넘을 것 같으면 독립적으로 검증 가능한 단위로 나누거나 이유를 설명하고 재승인받는다.
- 줄 수를 맞추려고 하나의 책임을 부자연스럽게 쪼개거나 서로 다른 개념을 묶지 않는다.
- 각 단위는 성공 조건 하나와 그에 맞는 확인 수단을 가진다

## 기술 기준

- 기본 클라이언트 스택은 Vite 8, React 18, TypeScript 6, `@apps-in-toss/web-framework` 3.x입니다.
- TDS를 사용할 때 `@toss/tds-mobile`과 `@toss/tds-mobile-ait`를 함께 사용하고, 앱 진입점은 `TDSMobileAITProvider`로 감쌉니다.
- 앱인토스 SDK는 각 앱의 `src/lib/` 브리지에서만 호출합니다. 화면 컴포넌트와 `App.tsx`에서 SDK를 직접 import하지 않습니다.
- SDK 함수명과 설정 타입은 기억이나 검색 결과가 아니라 해당 앱의 설치된 `node_modules/**/*.d.ts`와 공식 문서로 확인합니다. SDK 3.x 설정은 `apps-in-toss.config.ts`, `webView`, `webBundleDir`, `ait build` 기준을 사용합니다.
- `vite.config.ts`에서는 `aitDevtools.vite()`와 React 플러그인의 순서를 기존 검증된 설정에서 바꾸지 않습니다.
- WebView의 새로고침·바운스·오버스크롤·앞뒤 제스처 차단 설정과 `user-scalable=no`를 임의로 되돌리지 않습니다.
- SSR, `http://`·`ws://` 통신, 외부 입력의 `eval` 실행, 앱인토스 심사에 영향을 줄 수 있는 히스토리 조작을 사용하지 않습니다.

## 설계와 구현 원칙

- 제품별 흐름과 상태는 해당 앱 안에 둡니다. 아직 두 번 이상 검증되지 않은 코드를 공용 패키지나 거대한 추상화로 먼저 일반화하지 않습니다.
- 공통화가 필요해지면 SDK 브리지, TDS 래퍼, 테스트 유틸처럼 경계가 분명하고 실제로 반복 사용된 코드만 `packages/`로 추출합니다. 앱별 `appName`, 권한, 브랜드, 배포 설정과 제품 도메인은 각 앱에 남깁니다.
- UI는 순수 도메인 모듈과 앱인토스 브리지 사이의 경계를 유지합니다. 브라우저에서 테스트 가능한 순수 함수와 실기기에서만 검증할 SDK 동작을 섞지 않습니다.
- 개인정보가 포함될 수 있는 원문·사진·분석 결과를 로그, 테스트 fixture, 커밋 메시지에 남기지 않습니다. 필요한 테스트 자료는 비식별화하고 집계값만 기록합니다.

## 프로젝트 소스 구조

각 앱의 `src/`는 제품 기능을 찾기 쉽고 변경 범위를 판단하기 쉽도록 다음 의미를 기본으로 사용합니다.

```text
src/
  features/     앱별 기능 규칙·타입·정책
  components/   여러 화면에서 재사용하는 UI 모듈
  screens/      사용자 화면과 화면 단위 조합
  hooks/        React 상태·폼·화면 흐름 조합
  lib/          앱인토스 SDK, 저장소, 공유 같은 외부 어댑터
  test/         테스트, 공통 fake, 비식별 fixture
```

- 제품의 순수 규칙은 `components/`나 `screens/`에 넣지 않고 `features/<기능명>/` 아래에 둡니다. 기능명은 각 앱의 제품 문맥에 맞게 정합니다.
- 테스트 파일은 구현 파일 옆에 흩어놓지 않고 `src/test/` 아래에서 대상 기능·어댑터별로 묶습니다. 공통 테스트 도구가 실제로 두 곳 이상에서 반복될 때만 `src/test/`에 별도 모듈로 둡니다.
- `components/`는 표시와 사용자 입력에 집중하고, SDK 직접 호출은 `lib/` 어댑터의 인터페이스 뒤에 둡니다.
- 앱 규모가 작아 아직 파일이 없는 폴더는 빈 placeholder로 만들지 않습니다. 첫 모듈이 생길 때 해당 폴더를 추가합니다.

## 검증 명령

각 앱은 해당 프로젝트 디렉터리에서 실행합니다.

```bash
npm run lint
npm run build
```

`build`는 TypeScript 프로젝트 빌드, Vite 번들, 앱인토스 번들을 모두 포함해야 합니다. SDK 권한, 카메라, 저장, 알림, 광고처럼 브라우저 mock과 실기기 동작이 다를 수 있는 기능은 두 환경을 구분해 검증합니다.
